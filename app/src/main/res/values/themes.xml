<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme -->
    <style name="Theme.GPSApp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary color -->
        <item name="colorPrimary">@color/black</item>
        <!-- Darker variant for the status bar and action bar -->
        <item name="colorPrimaryDark">@color/black</item>
        <!-- Accent color for highlights -->
        <item name="colorAccent">@color/teal_200</item>
    </style>

    <style name="Theme.MyMaterialButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="cornerRadius">16dp</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

</resources>

